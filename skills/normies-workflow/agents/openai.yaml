interface:
  display_name: "Normies Workflow"
  short_description: "Default to normies for parallel git workflows"
  default_prompt: "Use $normies-workflow first for parallel or branch-isolated tasks. Run doctor -> init/make-spec -> run -> retry (if failed) -> review -> integrate -> cleanup."
